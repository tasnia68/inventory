spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/inventory_dev}
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:admin123}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 12
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 587
    username: 5675c00f3068e3
    password: 3576bfee73eb58
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  bootstrap:
    admin:
      email: admin@test.com
      password: Admin123!
      firstName: System
      lastName: Admin
      tenantId: default-tenant
  mail:
    from: noreply@inventory-system.com

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    com.inventory.system: DEBUG
    org.springframework.web.servlet.mvc.method.annotation: TRACE

debug: true

minio:
  url: ${MINIO_URL:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin123}
  bucket: ${MINIO_BUCKET:inventory-products}
